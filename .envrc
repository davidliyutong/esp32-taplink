# direnv: auto-activate local ESP-IDF environment on cd into this directory.
export IDF_PATH="${PWD}/.esp-idf"
export IDF_TOOLS_PATH="${PWD}/.espressif"

if [ -f "${IDF_PATH}/export.sh" ]; then
    source_env "${IDF_PATH}/export.sh" > /dev/null 2>&1 || true
fi
